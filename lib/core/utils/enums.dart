enum RegisterFileType {
  personalPhoto,
  cv,
  idFront,
  idBack,
  qualification,
  degreePath,
}

enum AppointmentResult { success, endBeforeStart, conflict }

// enum PaymentType { instapay, vodafoneCash, bank }
enum PaymentType { none, bank, wallet }

